<?php

// This backend is API-only — the frontend SPA is served separately by
// Nginx (see docker/nginx/). All application routes live in routes/api.php;
// the health check route is registered directly in bootstrap/app.php (/up).
